<% if logged_in? %>
  <script type="text/javascript">
    window.currentUser = <%= render(
      "api/users/user.json.jbuilder",
      user: current_user
    ).html_safe %>;
    window.something = 'some other thing';
    </script>
<% end %> 

<main id="root"><marquee>REACT COME BACK PLS</marquee></main>

<% if flash[:errors] %>
  <footer><%= flash[:errors] %></footer>
<% end %>